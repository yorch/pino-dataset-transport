import pinoDataSetTransport from '../src';

describe('index', () => {
  describe('pinoDataSetTransport', () => {
    it('should be defined', () => {
      expect(pinoDataSetTransport).toBeDefined();
    });
  });
});
